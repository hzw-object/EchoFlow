# EchoFlow 实时语音合成系统依赖

# Web 框架
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0

# LLM 客户端
openai>=1.3.0

# 语音合成
# TTS 库（可选，推荐使用）
# TTS>=0.20.0

# Edge-TTS（可选，免费替代方案）
# edge-tts>=6.1.0

# Piper TTS（本地快速TTS，速度快但音质一般）
piper-tts>=1.3.0  # Piper TTS Python 包（包含 onnxruntime）
onnxruntime>=1.16.0  # ONNX 运行时，用于加载 Piper 模型
# 如果需要 GPU 加速，使用: onnxruntime-gpu>=1.16.0（替换 onnxruntime）

# CosyVoice（阿里巴巴通义实验室，最佳音质，超低延迟，强烈推荐）
# 注意：CosyVoice 需要从 GitHub 安装，无法通过 pip 直接安装
# 安装方法：
# 1. git clone https://github.com/FunAudioLLM/CosyVoice.git
# 2. cd CosyVoice && pip install -r requirements.txt
# 3. 下载预训练模型到 pretrained_models 目录
# 核心依赖（CosyVoice 需要）：
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0
scipy>=1.11.0


# 工具库
python-dotenv>=1.0.0

